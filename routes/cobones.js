const {authnMW, authrMW} = require('../RBAC_Auth/models/auth');
const { Cobone } = require('../models/cobone');
const express = require('express');
const router = express.Router();



module.exports = router; 
