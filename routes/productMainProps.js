const {authnMW, authrMW} = require('../RBAC_Auth/models/auth');
const { ProductMainProp } = require('../models/productMainProp');
const express = require('express');
const router = express.Router();



module.exports = router; 
