const {authnMW, authrMW} = require('../RBAC_Auth/models/auth');
const { LinkedProduct } = require('../models/linkedProduct');
const express = require('express');
const router = express.Router();



module.exports = router; 
