const {authnMW, authrMW} = require('../RBAC_Auth/models/auth');
const { ComparedProduct } = require('../models/comparedProduct');
const express = require('express');
const router = express.Router();



module.exports = router; 
